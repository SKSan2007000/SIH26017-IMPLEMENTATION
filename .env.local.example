# Copy to .env.local and set this to point the frontend at a real FastAPI
# backend. Leave unset (or delete this file) to keep using mock data --
# that's the default and needs no configuration.
#
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
